---
import { ViewTransitions } from "astro:transitions";

import type { SiteMeta } from "@/types";
import BaseHead from "@/components/BaseHead";
import ThemeProvider from "@/components/ThemeProvider";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import SkipLink from "@/components/SkipLink";
import { siteConfig } from "@/site-config";

import "aos/dist/aos.css"; // AOS styles

interface Props {
	meta: SiteMeta;
}

const {
	meta: { title, description = siteConfig.description, ogImage, articleDate },
} = Astro.props;
---

<html lang={siteConfig.lang}>
	<head>
		<BaseHead title={title} description={description} ogImage={ogImage} articleDate={articleDate} />
		<ViewTransitions />
	</head>
	<body>
		<ThemeProvider />
		<SkipLink />
		<Header />
		<main id="main">
			<slot />
		</main>
		<Footer />
		<!-- animate on scroll script -->
		<script>
			import { aosInit } from "@/js/aos";
			aosInit(); // runs on initial page load
			document.addEventListener("astro:after-swap", aosInit); // runs on view transitions navigation
		</script>
	</body>
</html>
